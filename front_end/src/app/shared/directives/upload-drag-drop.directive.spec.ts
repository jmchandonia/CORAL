import { UploadDragDropDirective } from './upload-drag-drop.directive';

describe('UploadDragDropDirective', () => {
  it('should create an instance', () => {
    const directive = new UploadDragDropDirective();
    expect(directive).toBeTruthy();
  });
});
